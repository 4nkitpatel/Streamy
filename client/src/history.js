import { createBrowserHistory } from "history"; // came from react router dom package interally

export default createBrowserHistory(); // this will create a history obj and we will maintain it in our project
